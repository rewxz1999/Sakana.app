# ---- 依赖与构建产物 ----
node_modules/
out/
dist/
release/
*.tsbuildinfo
# 本地依赖缓存（含 electron 压缩包、libmpv dll 等，体积巨大且可重新下载）
.npm-cache/
.electron-cache/
.cache/

# ---- 大体积运行时不入库（用 npm run {libmpv,ffmpeg,aria2}:fetch 获取） ----
resources/libmpv/
resources/ffmpeg/
resources/aria2/*.exe
resources/aria2/*.zip
resources/ffmpeg/*.zip
resources/libmpv/*.zip
# N-API 原生插件的编译产物（源码在 native/mpv/src）
native/mpv/build/

# ---- 安全：绝不入库的凭据与本地配置 ----
# 注意：.npmrc 只含国内镜像地址（无凭据），因此**保留入库**，方便其他人直接安装依赖
.env
.env.*
*.local
*.key
*.pem
*.pfx
*.p12
secrets.json
**/credentials.json

# ---- 内部文档：按用户要求只保留在本地，不进仓库 ----
# （仓库里的 README 是缩略版；这几份是完整的项目说明、更新记录、审计报告与调研笔记）
docs/代码审计.md
docs/项目说明书.md
docs/更新记录.md
docs/备用数据源调研.md

# ---- 运行期数据、诊断输出与临时文件 ----
# v0.2.9 最后更新：数据根目录改到**安装目录**下（开发态就是仓库目录），
# 因此这几个运行期目录必须忽略，否则开发时跑一次就会把缓存/截图/下载内容带进提交。
data/
cache/
downloads/
screenshots/
galgame-screenshots/
logs/
*.log
.cap*.log
.tmp-*
.testmedia/
.shots/
.tstest/
.DS_Store
Thumbs.db
